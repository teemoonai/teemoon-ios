# The publishable zip, produced by the repackage procedure in VENDORING.md
# alongside the xcframework. The xcframework itself IS committed (via Git
# LFS); the zip is only useful if the artifact ever moves to a hosted
# binaryTarget(url:).
artifacts/*.zip
